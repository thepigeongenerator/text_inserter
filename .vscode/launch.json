{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/target/debug/txtinsert",
            "args": ["test.txt"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "rust: cargo build"
        },
    ]
}
